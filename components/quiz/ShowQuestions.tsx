import QuestionItem from "./QuestionItem"

const ShowQuestions = ({className}:{className?:string}) => {
  return (
    <section className={`w-[720px] flex flex-col gap-5 ${className}`}>

    </section>
  )
}

export default ShowQuestions  
